[profile.default]
src = "contracts"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# PulseChain mainnet RPC (public). Override with your own for deploys.
[rpc_endpoints]
pulsechain = "https://rpc.pulsechain.com"
