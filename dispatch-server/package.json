{
  "name": "dispatch-server",
  "version": "1.0.0",
  "description": "Small HTTP server that receives task dispatches and appends them to a queue file",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
